# DAT Decision Log

Append-only log of **durable product and architecture decisions**. For backlog slices, see [roadmap-todo.md](./roadmap-todo.md). For product narrative, see [docs/product/](../product/).

**Format:** Date | ID | Decision | Status | Batch | Notes

---

## Entries

| Date | ID | Decision | Status | Batch | Notes |
| ---- | -- | -------- | ------ | ----- | ----- |
| 2026-08-20 | DEC-069 | **Remote migration deploy requires purpose-scoped target validation and interactive human execution authorization:** Migrations remain human-operated. The canonical Production/operator path is `pnpm ops:migrate-deploy-remote`, which loads `.env.operator.production.local` via the existing `node --env-file` operator convention. Default invocation is **preflight only**: parse environment, validate target identity, print a redacted summary, perform **zero** database connection, and spawn **no** Prisma process. Target identity is validated before any Prisma write authority: expected pooled host (`DAT_OPS_EXPECTED_DB_HOST`), expected independent direct host (`DAT_OPS_EXPECTED_DIRECT_DB_HOST`), expected database name, and expected Supabase project ref are mandatory. `DATABASE_URL` is required, PostgreSQL-protocol only, and must match expected pooled host, database, and extractable project ref. `DIRECT_URL` is required on this path because Prisma schema declares `directUrl = env("DIRECT_URL")`; it must be PostgreSQL, parseable, and independently match expected direct host, expected database, and expected project ref. `DAT_OPS_EXPECTED_DB_HOST` and `DAT_OPS_EXPECTED_DIRECT_DB_HOST` are intentionally separate because pooled and direct hosts may legitimately differ. DIRECT_URL host is never compared to `DAT_OPS_EXPECTED_DB_HOST` and is never inferred from `DATABASE_URL`. Missing `DAT_OPS_EXPECTED_DIRECT_DB_HOST` fail-closes. `--execute` is separate from preflight. `--execute` refuses unattended-host signals (`CI`, `GITLAB_CI`, `VERCEL`), requires an interactive TTY, and requires a typed confirmation phrase derived from safe target identity (`MIGRATE <database> <project-ref-prefix>`). Confirmation is not persisted in `.env.operator.production.local` or any env token. Existing `remote-operator-target-guard.ts` inspect-only `ok: true` is **not** migration write authorization. Unattended CI/Vercel migration remains prohibited. Solution #3 / DEC-068 is unchanged. Destructive seed protections (DEC-062) are unchanged. Raw `source .env` + `prisma migrate deploy` is no longer the canonical Production path. | Accepted | `migration-deploy-target-safety-gate-v1` | Finding `DB-MIGRATION-001`. Guard: `lib/ops/migration-deploy-target-guard.ts`. Orchestration: `lib/ops/migration-deploy-remote.ts`. CLI: `scripts/migrate-deploy-remote.ts`. Package script: `ops:migrate-deploy-remote`. Expected direct host env: `DAT_OPS_EXPECTED_DIRECT_DB_HOST`. Implementation authorization is not migration-execution authorization. |
| 2026-08-18 | DEC-068 | **Ordinary repository-local development must use a loopback database target:** `env:check` fail-closes unless `DATABASE_URL` (and `DIRECT_URL` when present) independently parse as Postgres/PostgreSQL URLs whose host is on the existing local allowlist (`localhost`, `127.0.0.1`, `::1`, `*.localhost`). Unknown, malformed, empty-host, unsupported-protocol, Docker (`host.docker.internal`, `postgres`), RFC1918, and other non-local targets fail closed. `CI`, `GITLAB_CI`, and `NODE_ENV` are not remote-database authorization. The only hosted-runtime exemption in this slice is `VERCEL=1` (Vercel Preview/Production hosted env). Production/operator access remains explicit `.env.operator.production.local` plus `DAT_OPS_EXPECTED_*` identity guard and approved operator scripts. No generic escape hatch (`ALLOW_PROD_DB`, `ALLOW_REMOTE_DB`, `SKIP_DB_GUARD`, `DISABLE_DB_GUARD`, or similar). This does **not** make every Prisma CLI command safe: `prisma migrate deploy` / raw migrate safety remains `DB-MIGRATION-001` / `migration-deploy-target-safety-gate-v1`. | Accepted | `local-development-database-isolation-v1` | Guard: `lib/ops/local-development-database-guard.ts`; reuses `parseDatabaseTarget` / `isLocalDestructiveSeedHost` from DEC-062; wired through `scripts/env-check.ts`. Finding `CONFIG-ENV-001`. |
| 2026-08-14 | DEC-067 | **DAT migrates directly from Next 14 to Next 16 Active LTS for `DEP-SEC-001`:** New 2026 registry/security evidence showed `next@latest` = `16.3.1` (stable Active LTS, not prerelease). The previously planned 14.2.35 containment remains historically valid for the older 14.x CVE sequence, but is superseded as the remediation plan because Next 14 is unsupported and 14.2.35 is no longer sufficient containment. Next 15 Maintenance LTS was rejected as a short-lived intermediate migration. Exact selected versions: `next@16.3.1`, `react@19.2.8`, `react-dom@19.2.8`, `next-auth@4.24.15` (peer `next` includes `^16`), `eslint@9.39.5`, `eslint-config-next@16.3.1`, `@next/eslint-plugin-next@16.3.1`, `@types/react@19.2.18`, `@types/react-dom@19.2.4`. `next lint` is replaced by the ESLint CLI (`eslint .`) because Next 16 removed the built-in lint step. The global Engineering Excellence Audit remains CLOSED; this does not reopen discovery. Collapses/supersedes `next-security-patch-containment-v1` and `next-supported-lts-migration-v1` into `next-supported-lts-security-remediation-v1`. `dependency-security-monitoring-v1` and `TOOLCHAIN-002` remain separate. No database, hosted, or Production mutation. Rollback boundary: revert this branch to published `main` `ccdc8490904b63bddcb8875df86f24c43654796d` before merge; after merge, revert the merge commit. Validation boundary: local `pnpm check` (lint CLI, typecheck, 207/207 files, 1737/1737 tests, Next 16.3.1 default Turbopack production build). Canonical documentation and Super Agent continuity are updated in the same solution branch. | Accepted | `next-supported-lts-security-remediation-v1` | Starting baseline `ccdc849`; registry guard 2026-08-14; React 19 peer warnings recorded as residual; ESLint react-hooks v7 compiler rules kept as warnings to preserve the prior DAT error contract |
| 2026-08-04 | DEC-066 | **Node 24 is the DAT runtime baseline:** Repository runtime pins, package engines, root/app `.nvmrc`, GitLab CI and runner documentation are aligned to Node 24; local verification uses Node 24.18.0 and pnpm 10.24.0; Vercel Project Settings and effective Preview/Production runtime are Node 24.x. The migration merged to `main` at `909b69a`. Post-deploy fixture preflight, API guard and read-only UI smoke passed. Mutation smoke was intentionally not repeated because the slice changed runtime/configuration only; the earlier successful mutation trail remains the current write-path evidence and no additional Production write was justified. | Accepted | `node-24-runtime-closure-record-v1` | Migration commit `e5ddc39`; branch evidence commit `f23e423`; merge `909b69a`; GitLab branch and main pipelines passed with `node:24`; Vercel Preview and Production Ready; post-deploy gates **1/0/0 + API exit 0 + 4/0/0** |
| 2026-07-24 | DEC-065 | **School person identifier configuration belongs in Admin Settings (product surface):** Each organization must be able to configure school-facing person identifiers separately for (1) students and (2) instructors/staff under **Admin → Settings → Identifiers & numbering** (English UI baseline; PT planning label *Identificadores e numeração*). Distinguish technical DB ids, sequential `studentNumber`, presented school ids (`schoolStudentId` / `studentIdNumber`), future internal instructor number, and official `instructorLicenseNumber` (must not be reused as internal school number). This decision authorizes **planning** (`school-person-identifiers-settings-product-plan-v1`) only — **does not** claim the numbering system is implemented; schema/migrations/runtime remain gated. Plan must reconcile DEC-026 (Settings currently operator/internal / hidden from school admin nav) before exposing school-admin configuration. | Accepted | memory sync on `dat-production-smoke-canonical-fixtures-v1` | Existing code: `lib/students/student-school-id.ts` (hardcoded YY+NNN); tenant `@@unique([organizationId, schoolStudentId])`; global uniques on `studentIdNumber` / `studentNumber` / `instructorIdNumber` / `instructorLicenseNumber` need multi-tenant review in the plan |
| 2026-07-23 | DEC-064 | **Canonical DAT Production Smoke fixtures (corrected):** Technical smoke tenant uses fixed display identities. **Manual/legacy rename targets:** `Smoke Instructor 1` ← Michael Johnson/`INS-001-2024`; `Smoke Student 1` ← Alice Smith/`STU-001-2024`; negatives `Smoke Instructor Non-B` ← David/`INS-003-2024`, `Smoke Student A1` ← Carol/`STU-003-2024` (remote observed provenance may be `unknown` — name does not claim invite). **Invite fixtures:** `Smoke Instructor 2` and `Smoke Student 2` are valid **only** when created via the real invite+accept application flow with coherent `UserInvitation` ACCEPTED evidence (exact operator-only emails `DAT_SMOKE_INVITED_INSTRUCTOR_EMAIL` / `DAT_SMOKE_INVITED_STUDENT_EMAIL`; role; `acceptedUserId`; student `studentId`; category B; instructor booking+license; student `APP_USER`). Absence of coherent ACCEPTED invite is a **blocker** (`canonical_invited_*_missing`), not a warning — reconcile must not rename Sarah Williams/`INS-002-2024` or Bob Wilson/`STU-002-2024` into invite-canonical names. **Preserved additional (no rename/delete/merge):** Sarah Williams/`INS-002-2024`, Bob Wilson/`STU-002-2024` (inspector `additionalInstructor` / `additionalStudent`). Vehicles `01-DS-24`…`05-DS-24` (`03-DS-24` A1 negative). Smoke-only feature overrides on org `DAT Production Smoke`: `LESSON_MANAGEMENT`, `VEHICLE_MANAGEMENT`, `STUDENT_ACCESS`. CLI dry-run default / `--apply`; does not fabricate ACCEPTED rows, send email, or accept invites. Local seed may simulate post-accept ACCEPTED artifacts deterministically without outbound email. | Accepted | `dat-production-smoke-canonical-fixtures-v1` | Tooling: `lib/ops/production-smoke-fixtures-reconciliation.ts`; operator-safe `node --env-file=.env.operator.production.local`; remote writes are **human-controlled**. **Closure note (2026-07-28, human operator — no emails/full IDs in docs):** (1) repair CLI applied — ACCEPTED STUDENT invite `studentId` linked only; (2) fixture `--apply` completed (`changesApplied=18`); (3) read-only inspector: all readiness flags true, blockers none, invite fixtures `provenance=invite`, Sarah/Bob/John Doe preserved, informative-only `additional_school_admins_informative_only`; (4) second dry-run idempotent (features `noop`, 0 name changes, fixtures `canonical=true`). Commercial catalogue untouched; embedded `PLATFORM_ADMIN` not recreated. Agent did not execute remote repair/apply. Follow-up ops: `dat-production-smoke-hosted-verification-v1`. People Instructors list refresh bug remains `people-instructor-invite-accept-list-refresh-v1`. |
| 2026-07-17 | DEC-063 | **DAT vs autonomous MeEngine Platform boundary (smoke reconcile):** Do **not** recreate embedded DAT `PLATFORM_ADMIN` after the 2026-07-17 smoke DB reset. The future autonomous **MeEngine Platform** is the target authority for owner/admin access, products, tenant provisioning, domains, plans, subscriptions/payments, commercial catalogue, entitlements, and cross-product control-plane operations. **DAT** remains the school operational product (School Admin, instructors, students, vehicles, lessons, exams, school settings, DAT runtime features). Embedded DAT `/platform` runtime is **transitional and non-authoritative** — retain for now; do not treat it as the future Platform. Immediate ops focus is technical smoke tenant reconciliation (`dat-production-smoke-reconcile-v1`). Autonomous Platform separation planning is a dedicated future slice (`platform-separation-architecture-plan-v1`). Commercial catalogue read services must not resume blindly inside the embedded Platform. | Accepted | `dat-production-smoke-reconcile-v1` | Supersedes abandoned `platform-admin-access-and-smoke-reconcile-v1` direction for embedded Platform-admin restore; inspect-only tooling: `driving_school_platform/nextjs_space/docs/ops/production-smoke-reconciliation-inspect.md` |
| 2026-07-17 | DEC-062 | **Destructive database seed safety boundary:** The legacy development/demo seed (`scripts/seed.ts` / `prisma db seed`) is **local-only** and always **fail-closed** on remote database hosts (no remote bypass via `NODE_ENV`, `ALLOW_PROD_SEED`, FORCE, or similar). Local destructive execution requires the exact confirmation `ALLOW_DESTRUCTIVE_LOCAL_SEED=DELETE_LOCAL_DAT_APP_DATA` before Prisma construction. Remote technical environments must use dedicated non-destructive, idempotent operator commands. Destructive database commands must never appear in routine multi-command copy/paste close batteries; database-writing commands require isolated, explicitly approved blocks; agents must distinguish inspection / preview / apply; a failed database command must not be followed automatically by another database command in the same paste block. Git tags are code anchors only and do not restore database rows. | Accepted | `platform-commercial-catalog-seed-v1` | Guard: `lib/ops/destructive-seed-safety.ts`; incident: [2026-07-17-remote-legacy-seed-reset.md](../ops/incidents/2026-07-17-remote-legacy-seed-reset.md); commercial path remains `pnpm seed:commercial-catalog` with `--apply` |
| 2026-07-14 | DEC-061 | **Platform commercial catalogue schema foundation (additive, repo-only):** Hybrid catalogue tables and enums implemented in repository — global Platform-owned, provider-neutral; stable identities + versioned offerings; **product scope enforced at database level** via required `productId` on offerings/grants and compound foreign keys so offerings cannot reference plans/add-ons/catalogue versions from another product and grants cannot reference entitlement definitions from another product; relational `AddOnOfferingEligibility` (not arrays); grants FK to product-scoped `EntitlementDefinition`; prices use `amountMinor Int` with exactly-one target CHECK; typed grant value CHECK constraints; Class-B RLS + REVOKE on all 11 new tables. **No catalogue seed data; no runtime read/write services; no provider mappings; no subscriptions; not deployed by agent.** Published catalogue immutability remains a future write-service responsibility — DRAFT rows editable at app layer only. | Accepted | `platform-commercial-catalog-schema-foundation-v1` | Migration `20260714160000_platform_commercial_catalog_schema_foundation_v1`; contract test `lib/platform/commercial-catalog-schema-foundation.unit.test.ts`; implements DEC-060 plan; closes OD-015 |
| 2026-07-14 | DEC-060 | **Platform commercial catalogue invariants (planning):** Commercial catalogue is Platform-owned, global, and provider-neutral. Stable identities (`productKey`, `planKey`, `addOnKey`, `entitlementKey`) are separated from immutable/versioned commercial definitions (`CatalogueVersion`, `PlanOffering`, `CataloguePrice`, grants). Published catalogue versions, offerings, and prices must not be silently mutated — corrections create new versions. Historical subscriptions must pin to agreed `PlanOffering` + `CataloguePrice` (and add-on offerings where applicable), never display names or live catalogue rows alone. Candidate table/field names in [platform-commercial-catalog-schema-plan.md](./platform-commercial-catalog-schema-plan.md) are **proposed — not implemented**. | Accepted | `platform-commercial-catalog-schema-plan-v1` | Hybrid model (Option C) recommended; supersedes “exact composition TBD until schema plan” wording in DEC-058 notes only for planning status |
| 2026-07-14 | DEC-059 | **Canonical Super Agent memory and continuity contract:** Repository-backed canonical memory is the project recovery mechanism. `decision-log.md` owns durable history; `current-state.md` owns present truth; `roadmap-todo.md` owns ordered future work; `architect-mode.mdc` owns agent navigation, critical invariants, and next-slice guidance. Every completed slice must run a Memory Consistency Gate; verified human/operator actions must be reconciled; agents must not continue from stale pre-operation or superseded planning state. | Accepted | `dat-plan-naming-and-doc-hygiene-v1` | Section in [architect-mode.mdc](../../.cursor/rules/architect-mode.mdc); complements [cursor-operating-model.md](../ops/cursor-operating-model.md) |
| 2026-07-14 | DEC-058 | **DAT commercial plan display names:** DAT v1 uses three commercial plans — **DAT Core**, **DAT Plus**, **DAT Premium** — superseding planning display names Basic / Standard / Premium. Three-tier structure from DEC-048 remains valid. Planned stable internal keys: `DAT_CORE`, `DAT_PLUS`, `DAT_PREMIUM` (separate from `MONTHLY`/`ANNUAL`). Display names are **not** authorization keys; plan authorization derives from effective entitlements. Final prices and exact plan composition remain open. Premium must not be described as including every future capability forever. | Accepted | `dat-plan-naming-and-doc-hygiene-v1` | Supersedes DEC-048 **display names only** (DEC-048 append-only); [dat-plan-and-module-catalog.md](../product/dat-plan-and-module-catalog.md) |
| 2026-07-14 | DEC-057 | **Final DAT v1 release tagging:** Commercial DAT v1.0 ships via **release candidate** tags (`dat-v1.0.0-rc.1`, incrementing RC suffix) then an **immutable final** tag `dat-v1.0.0`. Hotfixes use `dat-v1.0.1`, `dat-v1.0.2`, etc. Tag creation and publication remain **human-controlled**; agents do not publish tags. See [git-tags-and-recovery-runbook.md](../ops/git-tags-and-recovery-runbook.md) and [dat-v1-commercial-release-plan.md](./dat-v1-commercial-release-plan.md). | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | Complements DEC-055/056; RC criteria in release plan |
| 2026-07-14 | DEC-056 | **DAT v1 core safety baseline tag:** Annotated tag **`dat-v1-core-baseline-95b833e`** points to commit **`95b833e`** — stable DAT v1 operational core **before** commercial Platform completion and smoke-identity/School Admin slice; recovery/comparison anchor only; **not** final `DAT v1.0.0`; **not** a database/deployment/secret backup. Human-created and pushed to `origin`. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [git-tags-and-recovery-runbook.md](../ops/git-tags-and-recovery-runbook.md) |
| 2026-07-14 | DEC-055 | **Published Git tags are immutable project anchors:** Use annotated tags (`git tag -a`); push explicitly; verify local/remote refs; **never move** a published release or baseline tag; corrected releases get a **new** tag; do not tag every feature slice; never routine `git reset --hard <tag>` or `git push --force` on `main`; tags do not replace DB/infrastructure backups. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [git-tags-and-recovery-runbook.md](../ops/git-tags-and-recovery-runbook.md) |
| 2026-07-14 | DEC-054 | **Platform target = reusable multi-product control plane:** Platform must evolve product-neutral, separately deployable (phased), authoritative for customer/product/subscription/entitlement/provisioning state, usable by DAT and future MeEngine products — **not** a big-bang rewrite. Partial runtime exists today (`/platform`, `/api/platform/organizations`); production-ready commercial control plane **not** claimed. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [platform-multi-product-control-plane-plan.md](./platform-multi-product-control-plane-plan.md); supersedes PA-004 planning posture |
| 2026-07-14 | DEC-053 | **Platform is authoritative for real tenant provisioning:** Paying customer tenants (including **A Conquistadora**) must be provisioned through Platform — fresh org ID, domain, users, credentials, subscription, entitlements — **not** by reusing smoke fixtures or smoke org IDs. Smoke tenant **`DAT Production Smoke`** remains technical-only (DEC-045). | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [first-client-onboarding-record.md](./first-client-onboarding-record.md); [dat-v1-commercial-release-plan.md](./dat-v1-commercial-release-plan.md) |
| 2026-07-14 | DEC-052 | **Platform subscription billing and school ledger are separate payment domains:** Tenant subscription (school pays DAT provider) is Platform-owned. School→student ledger/balances is an optional DAT module — distinct models, checkout, and authorization paths. Existing `Payment` Prisma model is **not** assumed suitable for either domain. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [platform-subscription-billing-entitlements-plan.md](./platform-subscription-billing-entitlements-plan.md); [dat-plan-and-module-catalog.md](../product/dat-plan-and-module-catalog.md) |
| 2026-07-14 | DEC-051 | **School-facing ledger is an optional DAT module:** Not mandatory for every school; entitlement-gated (`SCHOOL_LEDGER`); may be included in Premium and/or sold as add-on on lower tiers; separate from Platform subscription billing. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [dat-plan-and-module-catalog.md](../product/dat-plan-and-module-catalog.md) |
| 2026-07-14 | DEC-050 | **Commercial modules may be sold as add-ons:** Plans bundle modules; individual modules may attach to subscriptions as add-on items; effective entitlements = subscription + add-ons + approved overrides — not tenant-editable raw feature flags. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [dat-plan-and-module-catalog.md](../product/dat-plan-and-module-catalog.md) |
| 2026-07-14 | DEC-049 | **Monthly and annual billing intervals** are supported product concepts for tenant subscription; prices and provider mapping remain open. Platform owns interval on catalog prices; DAT License UI exposes choice at checkout. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | Open: proration, trial (OD-004/005 in release plan) |
| 2026-07-14 | DEC-048 | **Basic, Standard, and Premium** are the DAT v1 commercial plan tiers — entitlement bundles mapping to modules. Supersedes forward planning that used Basic/Starter vs Premium/Enterprise-only (DEC-004 historical). No final prices in planning batch. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [dat-plan-and-module-catalog.md](../product/dat-plan-and-module-catalog.md); **display names superseded by DEC-058** (DAT Core / DAT Plus / DAT Premium) — three-tier structure unchanged |
| 2026-07-14 | DEC-047 | **DAT License initiates; Platform authorizes paid activation:** School Admin may view plan and start checkout/upgrade/downgrade from DAT License UI, but **paid entitlements must come from authoritative Platform/provider processing** (webhook/event projection) — never from browser redirect alone. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [platform-subscription-billing-entitlements-plan.md](./platform-subscription-billing-entitlements-plan.md) |
| 2026-07-14 | DEC-046 | **Platform owns tenant subscription billing:** Primary DAT v1 payment domain = schools pay DAT provider/owner. Platform owns customers, products, plans, prices, subscriptions, checkout, provider refs, invoices/payment status refs, trials, grace, cancel, suspend, effective entitlements (commercial), and product-tenant registry. DAT displays/initiates License workflow only. | Accepted | `dat-v1-commercial-platform-cutline-plan-v1` | [dat-v1-commercial-release-scope.md](../product/dat-v1-commercial-release-scope.md); clarifies DEC-005 scope split |
| 2026-07-13 | DEC-045 | **Smoke tenant identity + School Admin terminology (DAT v1):** Production smoke/test organization canonical name is **`DAT Production Smoke`** on `www.meengine.io` (existing `DAT_SMOKE_*` fixtures preserved). Real customer **`A Conquistadora`** is a **future fresh tenant** — new org ID, host, users, credentials, license; must **not** reuse smoke fixtures. DAT v1 exposes one tenant administrator product role: **`School Admin`**; persisted enum remains **`SUPER_ADMIN`** (no `ADMIN` / `SCHOOL_ADMIN` migration). No owner/billing/ops admin split in v1; future role granularity needs demonstrated customer need. Platform onboarding contract uses `schoolAdmin*` fields; created user still `role: "SUPER_ADMIN"`. Operator rename: `pnpm ops:rename-production-smoke-org` (dry-run default; `DAT_SMOKE_RENAME_APPLY=true` for write; mutates `Organization.name` only). | Accepted | `dat-v1-smoke-tenant-school-admin-identity-v1` | `lib/users/user-role-label.ts`; `lib/ops/rename-production-smoke-organization.ts`; `scripts/rename-production-smoke-organization.ts`; supersedes smoke naming in DEC-039/040/043 canonical docs. **Implementation note (2026-07-13):** human operator completed production rename apply; verified name `DAT Production Smoke` (org `cmltn7vdl0000f8c4vxy6gcwx`, host `www.meengine.io`). Agent did not execute apply. |
| 2026-07-02 | DEC-044 | **Audit log tenant-aware foundation (plan first):** Before any runtime audit logging, define a tenant-aware foundation and smallest-safe implementation slices in docs. `audit_logs` already exists and is Class-B hardened (RLS + REVOKE), but has **no `organizationId`** and **no app write paths** today. Plan: evolve toward an explicit audit event record where tenant-scoped mutations carry `organizationId` (nullable only for platform-scoped events), actor attribution and request correlation are explicit, and payload is minimal/redacted. No schema/API/UI changes in the planning slice; schema/migration/write-path slices remain gated P2. | Accepted | `audit-log-tenant-context-foundation-plan-v1` | [audit-log-tenant-context-foundation-plan.md](./audit-log-tenant-context-foundation-plan.md); ties into DEC-032 cutline and DEC-043 onboarding record |
| 2026-07-01 | DEC-043 | **First client onboarding = docs-only operational record:** Controlled first B2B client onboarding is tracked via [first-client-onboarding-record.md](./first-client-onboarding-record.md) — checklist + fillable evidence template in git. **No** CRM, backoffice UI, or new tables in v1. **Dedicated** `Organization` + tenant host required; **A Conquistadora** remains temporary smoke/test tenant only (DEC-039/040) — real client must **not** reuse `DAT_SMOKE_*` fixture IDs or serve as mutation smoke target. Secrets (passwords, keys) stay in vault only. Pre-go-live checklist + post-go-live evidence; manual client smoke on client host; automated smoke stays on smoke tenant unless a future dedicated smoke-org batch says otherwise. Aligns with DEC-032 cutline and DEC-042 qualified categories in minimum data. | Accepted | `production-first-client-onboarding-record-v1` | [first-client-onboarding-record.md](./first-client-onboarding-record.md); memory sync in `current-state.md` / `roadmap-todo.md` |
| 2026-07-01 | DEC-042 | **Instructor qualified categories admin management:** School Admin manages instructor `qualifiedCategories` via People → Instructors → Edit Instructor (multi-select active categories). Operational persistence via `PATCH /api/admin/instructors/[id]` with `{ qualifiedCategoryNames: string[] }` (`Instructor.id`); Prisma M2M `_InstructorCategories` replace-all `set`; tenant + demo guards; `[]` clears all. Profile PUT remains on `/api/users/update`. No migration/schema change. Complements DEC-041 smoke readiness metadata. | Accepted | `instructor-qualified-categories-management-v1b` | `lib/instructors/instructor-qualified-categories.ts`; `lib/instructors/instructor-record-qualified-categories.ts`; `components/admin/instructor-records-manager.tsx` |
| 2026-07-01 | DEC-041 | **Production smoke testids + booking readiness metadata:** Add stable admin `data-testid` markers (`lib/smoke/smoke-testids.ts`) for optional UI assertions in readonly/fixture/mutation smoke. Expose `qualifiedCategoryNames` and `instructorLicenseExpiry` on `GET /api/admin/instructors/all?forBooking=true` only (no extra surface on historical filter lists). Fixture preflight and mutation readiness hard-fail before POST when category **B** (or `DAT_SMOKE_EXPECTED_LESSON_CATEGORY`) is missing or license expired when metadata is exposed; legacy API without fields logs WARN. Assign categories via People → Edit Instructor (`instructor-qualified-categories-management-v1b`) — no operator SQL for routine fixture maintenance. **Not** in `pnpm check`/CI. | Accepted | `production-smoke-e2e-testids-v1` | `lib/instructors/instructor-booking-list.ts`; `e2e/helpers/smoke-fixture-preflight.ts`; `e2e/helpers/smoke-admin-page-load.ts` |
| 2026-07-01 | DEC-040 | **Production lesson mutation smoke v1 (API-first, no cleanup):** `@mutations` suite creates and updates one DRIVING lesson on the temporary smoke tenant using explicit fixture IDs only. Requires fixture preflight green, hosted `DAT_E2E_ALLOW_PRODUCTION=true` + `DAT_SMOKE_ALLOWED_HOSTS` when not localhost, and **`DAT_E2E_ALLOW_PRODUCTION_MUTATIONS=true`**. Hybrid API + light Playwright (Schedule Map time visibility, `/admin/lessons` load). **No DELETE/cleanup** in v1 — lessons are immutable smoke trail. **Not** in `pnpm check` or default CI. Script: `pnpm e2e:smoke:mutations`. Optional `pnpm e2e:smoke:prod:full`. Modal edit UI and testids deferred. Current **`A Conquistadora`** on `www.meengine.io` is temporary smoke/test tenant only; future official client tenant must not inherit fixture assumptions. | Accepted | `production-smoke-e2e-lesson-mutations-v1` | `e2e/helpers/smoke-lesson-helpers.ts`; `e2e/production-smoke/lesson-mutations.spec.ts`; `assertProductionMutationsAllowed` |
| 2026-06-23 | DEC-039 | **Production mutation smoke requires fixture preflight and explicit IDs:** Zero-write fixture preflight (`@fixture-preflight`) must pass before production lesson mutation smoke. Uses explicit `DAT_SMOKE_ORG_ID`, student/instructor/vehicle IDs, and optional `DAT_SMOKE_EXPECTED_*` identity checks — **no broad search**, **no demo org** for production mutation smoke. Until a separate official client tenant exists, current **`A Conquistadora`** on `www.meengine.io` may be used temporarily as a production smoke/test tenant (not final client tenant). Future official client tenant must not reuse smoke fixture assumptions. Real customer operational records must not be used as mutable fixtures. Mutation dual opt-in: `DAT_E2E_ALLOW_PRODUCTION_MUTATIONS` (implemented in `production-smoke-e2e-lesson-mutations-v1`). Script: `pnpm e2e:smoke:fixture-preflight`. | Accepted | `production-smoke-e2e-fixture-preflight-v1` | `e2e/helpers/smoke-fixture-preflight.ts`; `e2e/production-smoke/fixture-preflight.spec.ts` |
| 2026-06-23 | DEC-038 | **Instructor license data must be explicit operational data:** School Admin must provide **instructor license number** and **expiration date** when creating/sending an **INSTRUCTOR** invitation. Values persist on `UserInvitation` until accept. Invitation acceptance uses stored license data on `Instructor` creation and **must not** generate `INVITE-PENDING-*` placeholders. Legacy pending invitations without license fields fail accept with actionable error (revoke + recreate). Existing placeholder instructor records are manual cleanup via **Edit Instructor**; `hasOperationalInstructorRecord` excludes placeholders. Public signup and DEC-037 auto-approve unchanged. | Accepted | `instructor-invite-license-fields-v1` | Migration `20260623120000_add_instructor_license_fields_to_invitations`; `lib/instructors/instructor-license-utils.ts` |
| 2026-06-23 | DEC-037 | **Invitation acceptance is an approval boundary:** Users created through **valid invitation acceptance** are **`isApproved=true` immediately** (including **INSTRUCTOR**). A School Admin invitation is an explicit approval action; invited instructors must not land in pending-approval login state. **Public/direct signup** approval policy remains **separate** (`isApproved: role !== "INSTRUCTOR"` on `POST /api/signup` unchanged in this batch). Email verification on accept unchanged (`isEmailVerified=true` — invite link proves email control). | Accepted | `instructor-invite-auto-approve-v1` | `lib/invitations/invitation-accept-service.ts` |
| 2026-06-12 | DEC-036 | **Production smoke automation (hybrid, read-only v1a):** Automated post-deploy smoke uses **API + Playwright** — `GET /api/health`, `POST /api/signup` blocked (`public_signup_disabled` / `demo_signup_disabled`), invalid login, admin core page loads, optional instructor dashboard, logout. **No persisted writes** in v1a. **Hosted/production** runs require `DAT_E2E_ALLOW_PRODUCTION=true` and hostname in `DAT_SMOKE_ALLOWED_HOSTS`; guards in `e2e/helpers/env-guards.ts`. **CI / `pnpm check` do not run production smoke by default.** Lesson create/edit/modal/practical-number automation **deferred** to `production-smoke-e2e-lesson-mutations-v1` on a **dedicated smoke tenant** (not demo org, not real client data). **No automatic cleanup/delete** in v1a. Scripts: `pnpm e2e:smoke:api`, `pnpm e2e:smoke:readonly`, `pnpm e2e:smoke:prod`. Runbook: `driving_school_platform/nextjs_space/docs/ops/production-smoke-e2e.md`. | Accepted | `production-smoke-e2e-readonly-v1` | `e2e/production-smoke/readonly.spec.ts`; `scripts/smoke-production-api.ts` |
| 2026-06-12 | DEC-035 | **Schedule Map lesson edit — modal UX:** Edit from Schedule Map opens in a **Dialog** inside the Dashboard/Schedule Map context (`EditLessonDialog` + `useEditLessonForm`); admin `/admin` and instructor `/instructor` dashboards wired via `ScheduleMap.onEditLesson`. Modal fetch uses existing `GET /api/admin/lessons/[id]`; save uses `PUT` + `buildAdminLessonUpdateRequestBody`; success calls `handleLessonBooked` / `refreshSchedule` (no `focusDate/scheduleRefresh` query params in modal flow). **`/admin/lessons/edit/[id]` retained** as deep link/fallback with v1b `buildScheduleReturnHref` unchanged. No persistence (DEC-033), practical number (DEC-034), schema, auth, or API contract changes. | Accepted | `calendar-lessons-edit-modal-ux-v1` | `hooks/use-edit-lesson-form.ts`; `components/admin/edit-lesson-dialog.tsx`; `parse-admin-lesson-detail-response.ts` |
| 2026-06-12 | DEC-034 | **DRIVING lesson edit — practical number on student change:** On `PUT /api/admin/lessons/[id]`, when `lessonType` is DRIVING and operational `studentId` changes, recalculate **only that lesson's** `practicalLessonNumber` via `getNextPracticalLessonNumber` for the new student (before `lesson.update`, while the row still belongs to the previous student). Preserve number when student unchanged; do not renumber other lessons for old or new student; non-DRIVING unchanged. Display label: `Practice No. {n}` via `getPracticalLessonNumberLabel`. No schema/migration; concurrency unique-index hardening remains deferred (`practical-lesson-counter-concurrency-hardening`). | Accepted | `calendar-lessons-edit-practical-number-reassign-v1` | `lesson-update-delete-service.ts`; `practical-lesson-counter.ts`; `LESSON_DETAIL_ACCESS_SELECT` |
| 2026-06-11 | DEC-033 | **Lesson edit PUT persistence (instructor/student):** `PUT /api/admin/lessons/[id]` must accept and persist instructor and student changes. **API contract:** `instructorId` in request body = **Instructor User.id** (same as create / form options); service resolves to `Instructor.id` FK. `studentId` = operational **Student.id**. Omit fields to leave existing FKs unchanged (no accidental clear). Instructor role cannot assign another instructor (`403`). Inactive/unavailable instructor rejected (`409`, aligned with create). **Edit form:** do not pass `lesson.instructorId` (Instructor row id) as `instructorUserId` — use `instructor.user.id`. Schedule Map refresh v1b unchanged; stale map after edit was a persistence gap, not refresh regression. **Modal edit** deferred to `calendar-lessons-edit-modal-ux-v1`. No schema/migration. | Accepted | `calendar-lessons-edit-persistence-refresh-fix-v1` | Helpers: `lesson-update-request-body.ts`; service: `lesson-update-delete-service.ts` |
| 2026-06-11 | DEC-032 | **Production-readiness cutline (controlled first B2B client):** DAT core is **production-ready enough** for controlled production under **invite-only**, **`PUBLIC_SIGNUP_ENABLED=false`**, and **no live billing/checkout** marketing. **P0** before go-live is **operational/deploy discipline** (credential policy, per-target `migrate deploy` + `pnpm check` + smoke), not new feature work. **Audit logs:** not P0; **planning** (`audit-log-tenant-context-foundation-plan-v1`) is **P1**; **runtime/migration/write paths** are **P2** unless client/compliance requires earlier. **`mobile-tablet-readiness-review-v1`** remains **penultimate/deferred** (before Competitive/Product Discovery). **`competitive-product-discovery-v1`** remains **after** production, cohesion, and mobile readiness (DEC-007). Baseline: main `5f41082`; Calendar/Lessons v1d closed. | Accepted | `production-readiness-cutline-doc-v1` | [production-readiness-cutline.md](./production-readiness-cutline.md) |
| 2026-06-03 | DEC-001 | **DAT** = school operational product; **Platform** = future vendor control plane for DAT customers (orgs, plans, entitlements, DAT subscription billing, domains). Platform is not a separate shipped product today. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | [dat-vs-platform-boundary.md](../product/dat-vs-platform-boundary.md) |
| 2026-06-03 | DEC-002 | School-facing DAT should not long-term expose raw System Settings / Feature Flags; demote/reposition; most ownership moves to future Platform. DAT keeps understandable School Settings only. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | Runtime unchanged; review done — [admin-settings-client-visibility-audit.md](./admin-settings-client-visibility-audit.md); next `admin-settings-client-visibility-hide-v1` |
| 2026-06-09 | DEC-029 | **Pending invitation email update (policy):** Changing email on a **`PENDING` `UserInvitation`** is **distinct** from Student/Instructor Change email (profile must exist for those). **Reject** updating `UserInvitation.email` while keeping the same `tokenHash` — old links sent/copied to the previous email would remain valid and accept into the new email. **Accept** policy: update email + **regenerate `tokenHash`** in one transaction; return new `inviteLink` once (like create); **no auto-resend email in v1**. Applies only to `PENDING` invitations; block ACCEPTED/REVOKED. Linked Student (`studentId`): sync `Student.email`, preserve `appAccessMode=INVITED`, no User yet. Unlinked Student/Instructor: invitation + token only. Tenant-scoped; demo guard. **Revoke + new invitation** remains safe fallback. API: `POST /api/admin/invitations/[id]/change-email`. Runtime in small slices (unlinked instructor → unlinked student → linked student). No schema/migration/accept/token-algorithm/email-provider changes in policy batch. | Accepted | `invitation-email-update-policy-doc-v1` | Policy: [invitation-email-update-policy.md](./invitation-email-update-policy.md); slices 2a–2c done: `invitation-email-update-unlinked-instructor-v1`, `invitation-email-update-unlinked-student-v1`, `invitation-email-update-linked-student-v1` |
| 2026-06-09 | DEC-028 | **Instructor Change email (policy + v1 runtime):** Canonical Instructor login email = `User.email` only (no `Instructor.email`). Change **only** via dedicated `POST /api/admin/instructors/[id]/change-email` — not `PUT /api/users/update`. Applies to Instructors **with profile** in any operational state (active, pending approval, deactivated). Change email **must not** alter `User.isApproved` or `Instructor.isAvailableForBooking`; no auto-reactivate. Pending INSTRUCTOR invitation **without profile** is **not** Instructor change-email — use invitation email update (DEC-029) when implemented, or revoke + new invitation as fallback. Runtime v1: global `User.email` uniqueness; block pending invitation on new email; revoke PENDING INSTRUCTOR invites on old email; invalidate DB sessions and password-reset/email-verification tokens; admin attestation `isEmailVerified=true` (aligned with DEC-024). JWT cookies may persist until expiry after change (same limitation as Student). No schema/migration. | Accepted | `people-management-instructor-email-change-v1` | Policy: [instructor-email-change-policy.md](./instructor-email-change-policy.md); pending invite: [invitation-email-update-policy.md](./invitation-email-update-policy.md) (DEC-029) |
| 2026-06-10 | DEC-030 | **Supabase Class-B RLS v1b (sliced plan):** Reject monolithic v1b migration on all 19 remaining tables. **Accept** revoke-only Class-B pattern per slice: `ENABLE ROW LEVEL SECURITY` + `REVOKE ALL FROM anon, authenticated`; **no** `CREATE POLICY`; **no** `FORCE ROW LEVEL SECURITY`. **B1** `supabase-rls-class-b-hardening-v1b-nextauth-v1` — `accounts`, `sessions`, `verification_tokens`, `users` (first SQL slice). **B2** tenant business revoke-only (12 tables). **B3** global reference + `user_preferences` (3 tables). **`supabase-rls-tenant-policies-v1`** is **separate P2** — tenant `CREATE POLICY` only if Data API tenant access is product-required; requires JWT/session helper analysis. Prisma-primary path unchanged; no `@supabase/supabase-js`. **Implementation note (2026-06-10):** B1 migration `20260610150000_supabase_rls_class_b_hardening_v1b_nextauth` merged main `edd73de` (feature `d579a1f`). **Deploy note (2026-06-10):** operator `migrate deploy` succeeded on validated target env; post-deploy 24 migrations up to date; `pnpm check` 163/1223/build OK. **Smoke note (2026-06-10):** B1 manual auth smoke matrix pass (operator-confirmed green). **Implementation note (2026-06-10):** B2 migration `20260610160000_supabase_rls_class_b_hardening_v1b_tenant_business_revoke` merged main `dd26d18` (feature `dce55c7`; 12 tables). **Deploy note (2026-06-10):** operator `migrate deploy` succeeded on validated target env; post-deploy 25 migrations up to date; `pnpm check` 163/1223/build OK. **Smoke note (2026-06-10):** B2 manual smoke matrix pass (operator-confirmed green). **Implementation note (2026-06-10):** B3 migration `20260610170000_supabase_rls_class_b_hardening_v1b_global_reference` merged main `cdfacf2` (feature `f63f19d`; 3 tables). **Deploy note (2026-06-10):** operator `migrate deploy` succeeded on validated target env; post-deploy 26 migrations up to date; `pnpm check` 163/1223/build OK. **Smoke note (2026-06-10):** B3 manual smoke matrix pass (operator-confirmed all green). **Final state (2026-06-10):** RLS Class-B v1b revoke-only **complete** — all 31 Prisma-mapped tables hardened; B1+B2+B3 deployed + smoke green on validated env. Revoke-only track closed; tenant `CREATE POLICY` remains P2 only. | Accepted | `supabase-rls-class-b-hardening-v1b-global-reference-deploy-record-v1` | Plan: [supabase-rls-class-b-hardening-v1b-plan.md](./supabase-rls-class-b-hardening-v1b-plan.md) |
| 2026-06-09 | DEC-027 | **Operational `organizationId` NOT NULL readiness:** Future DB `NOT NULL` applies only to the six operational tables (`students`, `instructors`, `vehicles`, `lessons`, `exams`, `lesson_requests`) after operator null-scope report shows zero NULLs and zero high-risk conflicts on the **target** environment. **`users.organizationId` stays nullable** (`PLATFORM_ADMIN` uses null). **Dual-scope tables stay nullable:** `billing_events`, `system_settings`, `feature_flags`, `configuration_history`. Validated environment (2026-06-09): 0 operational NULLs, 0 dry-run proposals — **no backfill apply required**. Migrations are a separate **D4** batch (`tenant-operational-organization-id-not-null-migrations-v1`) with human `migrate deploy`; re-run full gate battery immediately before each environment. **Implementation note (2026-06-10):** `tenant-operational-organization-id-not-null-migrations-plan-v1` documented D4 GO/NO-GO gate. **Implementation note (2026-06-10):** `tenant-operational-organization-id-not-null-migrations-v1` merged migration `20260610140000_make_operational_organization_id_required` + schema (main `1854d1b`). **Deploy note (2026-06-10):** operator `migrate deploy` succeeded on validated target env; post-deploy 0 operational NULLs; `User`/dual-scope remain nullable. | Accepted | `tenant-operational-organization-id-not-null-deploy-record-v1` | Readiness + gate: [tenant-operational-organization-id-not-null-readiness.md](./tenant-operational-organization-id-not-null-readiness.md); audit: [tenant-required-operational-organization-id-audit.md](./tenant-required-operational-organization-id-audit.md) |
| 2026-06-09 | DEC-026 | **Module gating vs internal config:** School-facing module visibility (navbar, `FeatureGate`, licensed surfaces) uses **License / Entitlements** (`OrganizationFeature`, `EntitlementGrant`, `useLicense`). `system_settings` and `feature_flags` admin CRUD remain **internal / operator / deferred Platform** surfaces until productized as understandable School Settings. Do not conflate `/admin/settings` Feature Flags tab with entitlements. | Accepted | `admin-settings-client-visibility-review-v1` | Audit: [admin-settings-client-visibility-audit.md](./admin-settings-client-visibility-audit.md); Fase B/C done (`admin-settings-client-visibility-hide-v1`, `admin-license-client-readonly-v1`) |
| 2026-06-03 | DEC-003 | **People UX:** prefer internal tabs on `/admin/users` before route split; target Students/Onboarding, Instructors/Onboarding, App accounts (temporary). `/admin/instructors` route split deferred (D4). | Accepted | `people-management-onboarding-reframe-v1` | Onboarding reframe done; App accounts demote deferred until row-level app access |
| 2026-06-03 | DEC-004 | **Import/export packaging:** Basic/Starter = provider-assisted onboarding + basic export; Premium/Enterprise = self-service UI, dry-run/apply, templates, history/audit, advanced validation. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | [packaging-and-entitlements.md](../product/packaging-and-entitlements.md); runbook deferred |
| 2026-06-03 | DEC-005 | **Payment Integration** in DAT means school-facing payments (balances, packages, receipts, providers later), not DAT customer subscription billing (Platform). | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | Likely Premium/Enterprise |
| 2026-06-03 | DEC-006 | **Multi-Language** entitlement is not real i18n; framework and pt-PT packs are future slices. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | `i18n-framework-planning-v1`, `language-pack-pt-PT-v1` |
| 2026-06-03 | DEC-007 | **Competitive discovery** is a named future slice only; no deep analysis in sync v1. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | `competitive-product-discovery-v1` |
| 2026-06-03 | DEC-008 | **Super-Agent:** separate facts (repo) vs inferences vs recommendations; may challenge external advice with evidence; no forced agreement; durable decisions here; assumptions in `product-assumptions.md`. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | [cursor-operating-model.md](../ops/cursor-operating-model.md) — Product Strategy Protocol |
| 2026-06-03 | DEC-009 | **Preview/Vercel:** deploy `meengine-dat` from **repo root** (Vercel Root Directory = `driving_school_platform/nextjs_space`); remove temporary Preview hosts from `organization_domains` by exact host when done; never delete production domains; do not commit `.vercel`. | Accepted | `product-roadmap-and-platform-boundary-sync-v1` | [cursor-operating-model.md](../ops/cursor-operating-model.md) — Preview and Vercel housekeeping |
| 2026-06-04 | DEC-010 | **People Profiles:** Students L2 tab is **Profiles** (not Records). Profile rows show origin + app-access badges; search-first with 15/page Load more (students server-side; instructors client-side on SSR list). **Pending invitations** list stays on Onboarding (not removed) until unlinked invites are fully covered on profiles. App accounts tab stays until row-level app access. | Accepted | `people-management-profiles-status-and-pagination-v1` | Invitations list removal deferred; instructor pending badge on profile deferred |
| 2026-06-05 | DEC-011 | **People student row UX:** School Admin manages **people**, not technical entities. Student row exposes one person-level **Edit Student** (dialog sections **Student profile** + **App access**; app account edit is subordinate inside App access) and **Delete** with strong confirmation. Reject competing row-level **Edit profile** + **Edit app account**. **Delete** is product-facing but must obey existing safe server policy (manual-only; no User/app-account delete). App account delete/unlink deferred (`people-management-app-account-unlink-policy-v1`). App accounts tab preserved until safe parity. Future instructor equivalent: Edit Instructor + Delete with profile/app-access sections. | Accepted | `people-management-row-app-access-v1` | Corrects rejected first implementation on same batch |
| 2026-06-05 | DEC-012 | **Unified Edit Student (refined):** Identity fields (name, phone) appear **once** in **Student profile**. **App access** is a collapsible inline section — **no nested full app-account dialog** and no duplicate identity fields. APP_USER save uses existing dual APIs (`PATCH` student + `PUT` user) from one **Save Student** action. **Delete** is always visible on the row; blocked students get an explanatory modal (server policy unchanged). Instructor unified editor deferred (`people-management-edit-instructor-unified-v1`). | Accepted | `people-management-row-app-access-v1` | QA refinement; no new API/schema |
| 2026-06-06 | DEC-013 | **Unified Edit Student (field placement):** For **APP_USER**, login email is read-only in **App access** only (not in Student profile). For **MANUAL_ONLY** / **INVITED**, operational email stays in Student profile. **INVITED** shows read-only App access section (invitation pending); revoke stays on row. Reject nested duplicate app-account editor and separate row-level app account actions on student rows. | Accepted | `people-management-row-app-access-v1` | Re-analysis after QA |
| 2026-06-06 | DEC-014 | **App access lifecycle (future):** **Remove app access** = disable/remove app login while **preserving** Student profile and all historical records (lessons, payments, audit). Re-enrollment reuses the **same** Student profile; **Reactivate app access** updates categories/transmission and reuses profile/account where safe, or sends invitation when needed. Not hard delete of User or history. Remove v1 implemented in `people-management-app-access-remove-v1`; reactivate remains future. | Accepted | `people-management-app-access-remove-v1` | See DEC-016 and [student-app-access-lifecycle-policy.md](./student-app-access-lifecycle-policy.md) |
| 2026-06-06 | DEC-015 | **Canonical student email (School Admin):** One email per student — operational and login are the same concept in People UI. No duplicated operational vs login fields. Current branch: read-only login email in App access; row shows canonical email once. Future **Change email** (`people-management-student-email-change-policy-v1`): validate uniqueness; update Student + linked User; revoke/invalidate old email access; handle pending invitations; verification/re-auth as required. | Accepted | docs-only in row-app-access correction | Change email not implemented in v1 |
| 2026-06-06 | DEC-016 | **Remove app access (v1):** Dedicated transactional endpoint `POST /api/admin/students/[id]/app-access/remove` — not `DELETE /api/users/delete`. Sets Student `MANUAL_ONLY` + `userId=null`; preserves User row with `isApproved=false`; invalidates sessions and pending invitations; copies User email to Student when Student email empty. UI action in Edit Student → App access only. Idempotent second call → 409 `student_app_access_already_removed`. Reactivate deferred (`people-management-app-access-reactivate-v1`). | Accepted | `people-management-app-access-remove-v1` | Policy doc: [student-app-access-lifecycle-policy.md](./student-app-access-lifecycle-policy.md) |
| 2026-06-06 | DEC-017 | **Reactivate app access (v1):** `POST /api/admin/students/[id]/app-access/reactivate` — re-links orphan User (same canonical email, STUDENT role, tenant, not linked elsewhere). Path B (no orphan User) → 409 `reactivate_orphan_user_not_found`; admin uses Send invitation. Never creates duplicate Student. Pending invitation blocks reactivate. Change email remains `people-management-student-email-change-policy-v1`. | Accepted | `people-management-app-access-reactivate-v1` | Category/transmission edit after reactivate via existing Edit Student |
| 2026-06-08 | DEC-018 | **Instructor People UX parity (v1):** Instructors → Profiles row uses **Edit Instructor** (not row-level **Edit app account**). Unified dialog sections: **Instructor profile** + **App access**. Login email read-only in App access; save via existing `PUT /api/users/update` (`role: INSTRUCTOR`). **Delete** visible on row but **always blocked** in v1 — no `DELETE /api/users/delete` from Profiles. Real delete → `instructor-delete-policy-v1`. Instructor app access lifecycle (remove/reactivate) deferred. App accounts demote deferred until parity is safer. | Accepted | `people-management-edit-instructor-unified-v1` | Mirrors DEC-011/012 for instructors |
| 2026-06-08 | DEC-019 | **Instructor delete/deactivate policy:** **Delete**, **Deactivate**, and **Remove app access** are separate. **Hard delete** only for zero-dependency / onboarding-error records via `DELETE /api/admin/instructors/[id]` — never `DELETE /api/users/delete`. **Deactivate** is the normal path when an instructor leaves the school (preserve history; future `instructor-deactivate-v1`). **Remove app access** is a deferred lifecycle batch, not delete/deactivate. App Accounts demote waits until deactivate is implemented. Policy: [instructor-delete-policy.md](./instructor-delete-policy.md). **Implementation note (2026-06-08):** `instructor-hard-delete-zero-deps-v1` implements zero-deps hard delete + Profiles UI + legacy INSTRUCTOR guard on `DELETE /api/users/delete`. | Accepted | `instructor-hard-delete-zero-deps-v1` | Hard delete implemented; deactivate next |
| 2026-06-08 | DEC-021 | **App Accounts demote (People UX):** School Admin manages people via Students/Instructors — not technical accounts. App Accounts L1 tab removed from `/admin/users`. **Advanced accounts** collapsible section (collapsed by default) provides read-only diagnostics (name, email, role, approval, linked/unlinked). No UI for `POST /api/users/create`, `PUT /api/users/update`, or `DELETE /api/users/delete` in Advanced accounts. Constants: `lib/people/people-management-ui.ts`. **Implementation note (2026-06-08):** `users-delete-student-guard-v1` adds **409** `use_student_delete_policy` on `DELETE /api/users/delete` for STUDENT — legacy endpoint no longer deletes STUDENT or INSTRUCTOR Users. | Accepted | `people-management-app-accounts-demote-v1` | Change email / tenant scope still deferred |
| 2026-06-08 | DEC-022 | **Student operational fields on profile:** License category (`Student.categoryId`) and transmission type (`Student.transmissionTypeId`) are **school operational data** on the Student profile — edited via `PATCH /api/admin/students/[id]` (`categoryName`/`transmissionTypeName`) for all access modes (MANUAL_ONLY, INVITED, APP_USER). **App access** sections cover login status, invitations, remove/reactivate — not category/transmission. UI label **License category** (singular; model supports one `categoryId`). Row badges read profile fields, not linked User bridge. **Superseded for address placement by DEC-023.** | Accepted | `people-management-student-profile-operational-fields-v1` | Address moved to profile per DEC-023 |
| 2026-06-08 | DEC-023 | **Address on operational person profiles:** `Address` is **profile/contact data** for Students and Instructors (alongside name, phone, email) — not App access lifecycle UI. **Student:** `Student.address` (nullable) is the operational field for all access modes; `PATCH`/`POST /api/admin/students` accept `address`; read uses `student.address ?? linkedUser.address` during transition; **APP_USER** save syncs `User.address` from profile. **Instructor:** persists on `User.address` (required `userId`); UI field lives in **Instructor profile** only. **App access** = login email, approval/status, invitations, remove/reactivate/deactivate — not address. Import/export of address deferred. | Accepted | `people-management-profile-address-fields-v1` | Change email still deferred |
| 2026-06-08 | DEC-024 | **Student Change email (v1):** School Admin changes canonical student email via `POST /api/admin/students/[id]/change-email` only — not generic PATCH/PUT. **MANUAL_ONLY** (no user, no pending invite): updates `Student.email` with uniqueness checks. **INVITED:** revokes pending invitations, updates email, sets `MANUAL_ONLY`; admin re-sends invitation manually. **APP_USER:** atomic `Student.email` + `User.email`; invalidates sessions/tokens; admin attestation (`isEmailVerified=true`). **Post-remove MANUAL_ONLY:** updates `Student.email` only; orphan User unchanged. PATCH email allowed only for eligible MANUAL_ONLY; otherwise **409** `use_change_email_flow`. JWT sessions may persist until expiry (documented). No schema/migration. | Accepted | `people-management-student-email-change-policy-v1` | Instructor policy: DEC-028; auto-resend invite + notification deferred |
| 2026-06-10 | DEC-031 | **Calendar/Lessons polish (sliced plan):** Reject monolithic `calendar-lessons-polish-v1` runtime batch. Two surfaces: **Schedule Map** (`/admin`) and **Lesson Management** (`/admin/lessons`). Schedule Map type colors are correct (Theory green, Driving blue, Theory exam yellow, Practical exam orange). **v1a:** UI + SSR mapping — **done**. **v1b:** edit refresh/focus — **done**. **v1c-a:** honest Recent/Upcoming truncation — **done**. **v1c-b:** Upcoming horizon 7 days — **done**. **v1c pagination/load-more:** deferred. **v1d:** vehicle warnings — **done** (`calendar-lessons-polish-v1d-vehicle-warnings`): display-only; `LESSON_LIST_VEHICLE_SELECT` adds `isActive`, `underMaintenance`, `status`; warnings from persisted nested vehicle fields (not derived fleet status); no create/update blocking; expiry/service deferred. **v1e (deferred):** student warnings — product policy first. | Accepted | `calendar-lessons-polish-v1d-vehicle-warnings` | v1e deferred; mobile-tablet-readiness-review-v1 penultimate pre-discovery |
| 2026-06-09 | DEC-025 | **Instructor invitation vs profile lifecycle:** Pending **INSTRUCTOR** invitations live in **Onboarding** only — before a `User`/profile exists (`createInvitation` blocks `user_already_exists`). **Instructor Profiles** show post-account states only: `App access pending approval`, `Active`, or `Inactive`. No `Pending invite` badge on Profiles in v1; parity with Students linked invites deferred until manual instructor-without-User or linked-invite model exists. UI copy and label guide document the split. **Students vs instructors:** Students can show `Pending invite` on Profiles when `INVITED`; instructors appear on Profiles only after registration. | Accepted | `people-management-instructor-profile-invite-badge-v1`, `people-management-ux-unification-instructor-invitations-v1` | Linked instructor invites on profile deferred |
| 2026-06-08 | DEC-020 | **Instructor deactivate/reactivate semantics:** Deactivate preserves all operational history. Sets `Instructor.isAvailableForBooking=false` and `User.isApproved=false`; clears DB sessions; revokes pending INSTRUCTOR invitations for email. Future scheduled lessons **warn** but do not block; no auto-cancel/reassign. Inactive instructors remain visible in People; excluded from new booking (`forBooking=true`, lesson-create guard). **UX (pre-merge correction):** Deactivate/Reactivate managed inside **Edit Instructor → App access** (not row-level Deactivate); lessons with inactive assigned instructor show Schedule Map / admin list warning while remaining visible. **Reactivate** included in same branch: `POST /api/admin/instructors/[id]/reactivate` restores booking + login. **Badge consistency (pre-merge refinement):** People/Instructors reuse app-wide semantics — **Active/Inactive** row badges match Vehicles (`default`/`secondary`); Edit App access section uses shared blue theme + **Access status** labels matching Students (`Approved — can sign in`, `Pending approval`, `Inactive`); pending row badge matches Students app-access pending (`default`). Shared constants: `lib/people/people-app-access-ui-theme.ts`. App Accounts demote deferred. **JWT limitation:** existing JWT sessions may persist until expiry after deactivate; new credentials logins blocked via `isApproved` check. | Accepted | `instructor-deactivate-v1` | DEC-019 implementation slice |
