# Copy to .env.local and fill in real values (never commit secrets).
# See docs/ops/environment-variables.md

# Required for local dev (see lib/env.ts / env-check)
# DATABASE_URL=
# NEXTAUTH_SECRET=

# Optional. Enables public self-registration. Default is disabled.
# PUBLIC_SIGNUP_ENABLED=false
